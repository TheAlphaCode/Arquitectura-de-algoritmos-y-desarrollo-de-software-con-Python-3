n = int(input('Ingrese el exponente: '))# Valor limite o tope de calculo de la maxima potencia de n
x = float(input('Ingrese la base: ')) #Valor que sera elevado a la potencia n

if(n < 0):
    print('El numero n: {} no es un exponente positivo.'.format(n))
else:
    # Inicia el resultado de las sucesivas multiplicaciones de la base x
    r = x # Iguala el resultado a la base
    
    for i in range(1,n): # Ciclo que controla las multiplicaciones sucesivas de la base 
        r = r * x
    print('\nEl valor del resultado de: {}, elevado a la potencia {}'.format(x,n))
    print('\nEs igual a: {}'.format(r))
